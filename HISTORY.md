Make motion12 controls scrollable in the bottom panel and switch arrow rendering from r1 to raw accelerometer values.
Add a zoom-out slider to motion12 with a default 5x zoom-out applied to the graph scale.
Pin motion12 controls to the bottom half, move the dot/crosshair origin to the top-half center, and add a manual Reset v1 button.
Add m8-drone-control.html for 8-channel MIDI chord morphing.
Improve M8 output reconnection handling and add per-channel MIDI out monitor.
Document m8-drone-control note-change logic in the readme.
Modularize m8-drone-control into section JS files with shared styles.
Expand drone4 voice interval slider ranges to support 50ms to 10,000ms changes.
Add a lush built-in Web Audio drone synth to drone4 that mirrors played notes while preserving MIDI out behavior.
