modules:
  - motion:
      desc: learns forward direction using combo of GPS and accelerometer and gyro
      in:
        - GPS
        - accelerometer
        - gyro
      out:
        - normalized velocity (-1.0 to 1.0) forward/backward
      flow:
        - 
  - notes:
      desc: decides what the current note(s) are that play through the synth
      in:
        - 